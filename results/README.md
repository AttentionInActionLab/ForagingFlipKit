When executing the experiment locally, most browsers "download" the result file into your Downloads folder. So you might need to copy it manually. The suggested location is th"e "results" folder.

When executing online via JATOS, the results are stored on the server and can be downloaded as single .json file.
